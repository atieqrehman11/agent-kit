---
spec: {{SLUG}}
gate: 8 Report
status: final
derived-from: tasks.md@{{TASKS_HASH}}
---

# Delivery report — {{TITLE}}

| | |
|---|---|
| **Verdict** | PASS / PASS_WITH_CONDITIONS / BLOCKED |
| **Branch** | `deliver/{{SLUG}}` |
| **Requirement** | one line, as the user stated it |
| **Repo type** | api / pipeline / job / agent / genie / frontend / mixed |
| **Spec** | `requirements.md` · `design.md` · `tasks.md`, in this folder |

This report **does not restate the spec**. Criteria, decisions and tasks live in the three
documents beside it and are referenced by number. What belongs here is only what the build
learned: what was met, what the review said, what the tests did, and where reality diverged
from the plan.

---

## Spec provenance

Which documents this run read, and which it wrote. A `re-derived` row means the upstream
document changed after this one was written and it had to be rebuilt — say what changed.

| Document | Loaded / Derived / Re-derived | Note |
|---|---|---|
| `requirements.md` | | |
| `design.md` | | |
| `tasks.md` | | |

## Acceptance criteria

Every `AC` from `requirements.md`, with evidence. Evidence is a location or a test, never a
claim.

| # | Met | Evidence |
|---|---|---|
| AC1 | ✓ / ✗ | file:line, or the test that proves it |

**Not done:** rows from *Explicitly not doing* that a reader might expect to have been built
anyway, and anything dropped during the build that was not already on that list.

## Standards applied

Guidelines loaded at gate 1, and any place the existing code diverges from them. Divergence
is reported, not fixed mid-feature.

| Guideline | Divergence found in existing code |
|---|---|

## Where the build diverged from the design

Empty is a valid answer and a good one. A design that survived contact unchanged should say
so rather than leaving this section out.

| # | Design said | Build did | Why |
|---|---|---|---|
| D1 | | | |

**Tasks changed during build:** from the *Changes during build* table in `tasks.md`, or
"none".

**Risks that materialised:** from `design.md`, plus any that were not foreseen.

## Changes

| File | Added / Changed | What |
|---|---|---|

## Review — verbatim

Reviewer verdict, unedited, including anything unflattering. Fix rounds used: _n_ of 3.

```
VERDICT: …
```

**Outstanding findings** (only if BLOCKED): what remains, and what was tried.

**Disagreements** (if any): the finding as written, then why it was not actioned. No edits to
the finding itself.

## Tests

Real output, pasted. Not a summary.

```
$ <test command>
…
```

| Coverage gap | Acceptable? | Why |
|---|---|---|

## For the human

- **Look at this first:** the one thing most worth a human eye.
- **Not verified:** anything the tests do not actually prove.
- **Next step:** what to do with this branch.
