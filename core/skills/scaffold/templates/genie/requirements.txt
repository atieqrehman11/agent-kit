# deploy.py resolves the space with w.genie.list_spaces and calls create_space /
# update_space. Those were verified against 0.102.0; older SDKs predate the Genie
# management API and fail at import or attribute lookup.
databricks-sdk>=0.102.0
pyyaml>=6.0
