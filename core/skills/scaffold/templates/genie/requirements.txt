# The build is offline — it reads src/ and databricks.yml and writes JSON. The
# only dependency is the YAML parser. Reconciling the space is `databricks bundle
# deploy`, so there is no SDK call anywhere in this repo and no workspace token
# in CI.
pyyaml>=6.0
