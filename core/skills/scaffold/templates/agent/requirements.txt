# The reconciler calls /api/2.1/supervisor-agents through the SDK's raw
# api_client rather than a typed service, so any SDK new enough to carry
# WorkspaceClient works — there is no Beta-service version floor to track.
#
# The deploy job installs pyyaml itself (resources/deploy.job.yml declares it as
# a serverless environment dependency); the SDK is already present in the
# serverless runtime. This file is for running the reconciler on a laptop.
databricks-sdk>=0.30.0
pyyaml>=6.0
