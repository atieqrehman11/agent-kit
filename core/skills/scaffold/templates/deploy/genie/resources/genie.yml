# title/description live here; the content is the built artifact under generated/,
# with the catalog baked in per target. Renaming the key loses the space id.
resources:
  genie_spaces:
    TPLVAR_RESOURCE_KEY:
      title: ${var.space_title}
      warehouse_id: ${var.warehouse_id}
      file_path: ../generated/space.${bundle.target}.json
      description: |-
        TPLVAR_DESCRIPTION

        **Capabilities:**

        - TODO: what this space can answer, one bullet per question shape.

        **Limitations:**

        - TODO: what it cannot answer. Shown to users; be specific.
