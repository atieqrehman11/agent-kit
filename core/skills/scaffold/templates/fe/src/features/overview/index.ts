// The registry lazy-imports this file, so the default export is the feature's
// entry point. Everything else in the folder stays private to it.
export { OverviewPage as default } from './components/overview-page'
